# {{.ProjectName}} - AI Agent (ADK Framework) Makefile

.PHONY: help install run test lint format build deploy clean docker-build docker-run

# Default target
help: ## Показать справку по командам
	@echo "Доступные команды для {{.ProjectName}}:"
	@echo ""
	@grep -E '^[a-zA-Z_-]+:.*?## .*$$' $(MAKEFILE_LIST) | sort | awk 'BEGIN {FS = ":.*?## "}; {printf "\033[36m%-20s\033[0m %s\n", $$1, $$2}'

install: ## Установить зависимости
	@echo "📦 Установка зависимостей..."
	@if [ -f pyproject.toml ]; then \
		echo "Используем uv для установки зависимостей..."; \
		uv sync; \
	else \
		echo "Используем pip для установки зависимостей..."; \
		pip install -r requirements.txt; \
	fi
	@echo "✅ Зависимости установлены"

run: ## Запустить агента
	@echo "🤖 Запуск {{.ProjectName}} агента..."
	python -m src.agent

test: ## Запустить тесты
	@echo "🧪 Запуск тестов..."
	pytest tests/ -v

lint: ## Проверить код
	@echo "🔍 Проверка кода..."
	flake8 src/ tests/
	mypy src/

format: ## Форматировать код
	@echo "✨ Форматирование кода..."
	black src/ tests/
	isort src/ tests/

build: ## Собрать проект
	@echo "🔨 Сборка проекта..."
	python -m build

deploy: ## Развернуть проект
	@echo "🚀 Развертывание проекта..."
	# Добавьте команды развертывания здесь

clean: ## Очистить временные файлы
	@echo "🧹 Очистка..."
	find . -type f -name "*.pyc" -delete
	find . -type d -name "__pycache__" -delete
	find . -type d -name "*.egg-info" -exec rm -rf {} +
	rm -rf build/ dist/ .coverage htmlcov/

docker-build: ## Собрать Docker образ
	@echo "🐳 Сборка Docker образа..."
	docker build -t {{.ProjectName}} .

docker-run: ## Запустить в Docker
	@echo "🐳 Запуск в Docker..."
	docker run -p 8000:8000 --env-file .env {{.ProjectName}}

docker-compose-up: ## Запустить с docker-compose
	@echo "🐳 Запуск с docker-compose..."
	docker-compose up -d

docker-compose-down: ## Остановить docker-compose
	@echo "🐳 Остановка docker-compose..."
	docker-compose down

# Development targets
dev-install: install ## Установить зависимости для разработки
	pip install -r requirements.txt

dev-run: ## Запустить в режиме разработки
	@echo "🔧 Запуск в режиме разработки..."
	python -m src.agent

# CI/CD targets
ci-test: test lint ## Запустить все проверки для CI
	@echo "✅ Все проверки пройдены"

ci-build: clean build ## Собрать для CI
	@echo "✅ Сборка для CI завершена"
