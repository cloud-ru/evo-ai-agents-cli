# {{.ProjectName}}

AI агент для автоматизации задач и интеллектуальной обработки данных.

## 🚀 Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd {{.ProjectName}}

# Установка зависимостей
make install
# или
pip install -r requirements.txt
```

### Настройка

1. **Скопируйте файл конфигурации:**
   ```bash
   cp .env.example .env
   ```

2. **Отредактируйте `.env` с вашими данными:**
   ```bash
   nano .env
   ```

3. **Настройте API ключи для AI сервисов:**
   ```bash
   # OpenAI
   OPENAI_API_KEY=your_openai_api_key_here
   
   # Anthropic
   ANTHROPIC_API_KEY=your_anthropic_api_key_here
   ```

### Запуск

```bash
# Локальный запуск
make run
# или
python src/agent.py

# Docker запуск
make docker-run
# или
docker-compose up
```

## 📋 Команды

- `make install` - Установка зависимостей
- `make test` - Запуск тестов
- `make lint` - Проверка кода линтером
- `make run` - Запуск агента
- `make docker-build` - Сборка Docker образа
- `make docker-run` - Запуск в Docker

## 🤖 Возможности агента

- **Обработка текста**: Анализ и обработка текстовых данных
- **Автоматизация задач**: Выполнение рутинных операций
- **Принятие решений**: Интеллектуальный анализ и выбор решений
- **Интеграция с API**: Работа с внешними сервисами
- **Обработка данных**: Анализ и структурирование информации

## 🏗️ Разработка

### Требования
- Python 3.9+
- Docker (опционально)

### Структура проекта

```
{{.ProjectName}}/
├── src/                    # Исходный код
│   ├── __init__.py
│   └── agent.py           # Основной модуль агента
├── tests/                  # Тесты
├── .env.example           # Шаблон переменных окружения
├── requirements.txt       # Python зависимости
├── pyproject.toml        # Конфигурация проекта
├── Dockerfile            # Docker конфигурация
├── docker-compose.yml    # Docker Compose
├── Makefile              # Команды разработки
└── README.md             # Этот файл
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
make test

# Запуск с покрытием
pytest --cov=src tests/
```

## 🚀 CI/CD

{{if eq .CICDType "gitlab"}}![GitLab CI](https://img.shields.io/badge/GitLab-CI-orange){{else if eq .CICDType "github"}}![GitHub Actions](https://img.shields.io/badge/GitHub-Actions-blue){{else}}![CI/CD](https://img.shields.io/badge/CI/CD-Available-green){{end}}

Проект настроен для автоматической сборки и тестирования в CI/CD пайплайне.

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 🆘 Поддержка

- 📧 Email: support@cloud.ru
- 📖 Документация: [Cloud.ru Documentation](https://cloud.ru/docs)
- 🐛 Баги: [GitHub Issues](https://github.com/cloud-ru/{{.ProjectName}}/issues)
