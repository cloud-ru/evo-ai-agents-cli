# {{.ProjectName}} - AI Agent (ADK Framework)

{{.Description}}

Этот проект создан с использованием **ADK (Agent Development Kit)** - фреймворка для разработки AI агентов от Cloud.ru.

## 🚀 Возможности

- **ADK Framework** - современный фреймворк для AI агентов
- **FastAPI** - высокопроизводительный веб-фреймворк
- **Docker** - контейнеризация приложения
- **CI/CD** - автоматизированное развертывание
- **Мониторинг** - встроенные инструменты наблюдения

## 📋 Требования

- Python 3.9+
- Docker
- Git

## 🛠 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd {{.ProjectName}}
```

2. Установите зависимости:
```bash
make install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл
```

4. Запустите приложение:
```bash
make run
```

## 🏗 Разработка

### Доступные команды

```bash
make help          # Показать все доступные команды
make install        # Установить зависимости
make run            # Запустить приложение
make test           # Запустить тесты
make lint           # Проверить код
make format         # Форматировать код
make build          # Собрать Docker образ
make deploy         # Развернуть приложение
```

### Структура проекта

```
{{.ProjectName}}/
├── src/
│   ├── __init__.py
│   ├── agent.py          # Основной агент
│   ├── handlers/         # Обработчики событий
│   ├── tools/            # Инструменты агента
│   └── config/           # Конфигурация
├── tests/                # Тесты
├── docker/               # Docker конфигурация
├── .github/              # GitHub Actions
├── .gitlab-ci.yml        # GitLab CI
├── Dockerfile
├── docker-compose.yml
├── Makefile
└── README.md
```

## 🔧 Конфигурация

Основные настройки находятся в файле `.env`:

```bash
# ADK Configuration
ADK_AGENT_NAME={{.ProjectName}}
ADK_AGENT_VERSION=1.0.0
ADK_AGENT_DESCRIPTION="{{.Description}}"

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=true

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json
```

## 🚀 Развертывание

### Docker

```bash
make build
make run
```

### Docker Compose

```bash
docker-compose up -d
```

### CI/CD

Проект поддерживает автоматическое развертывание через:
- **GitHub Actions** (`.github/workflows/ci.yml`)
- **GitLab CI** (`.gitlab-ci.yml`)

## 📊 Мониторинг

ADK предоставляет встроенные инструменты мониторинга:
- Метрики производительности
- Логирование событий
- Трассировка запросов
- Алерты и уведомления

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 👥 Авторы

- **{{.Author}}** - *Изначальная разработка*

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
- Создайте Issue в репозитории
- Обратитесь к документации ADK
- Свяжитесь с командой разработки

---

**Создано с помощью [ai-agents-cli](https://github.com/cloud-ru/evo-ai-agents-cli)**
